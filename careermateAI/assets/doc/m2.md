# CareerMate AI — Task 3 合規性檢查（已完成部分）

> 對照 `prd.md` Task 3 要求，檢查 Navbar + Hero 已完成區塊，列出所有需要修改的 HTML 和 CSS。

---

## 問題總覽

| # | 類型 | 問題 | 影響範圍 |
|---|------|------|---------|
| 1 | HTML class 命名 | `btn-hero-primary` / `btn-hero-secondary` 不符合 `.btn .btn-primary` 規範 | Hero |
| 2 | HTML class 命名 | `btn-start` / `btn-signin` 不符合 `.btn .btn-*` 規範 | Navbar |
| 3 | CSS 結構 | 無 `.btn` 基礎類，按鈕公共樣式分散重複 | styles.css |
| 4 | HTML 結構 | `pain-cards` 放在 `.contact` section 內，位置錯誤 | index.html |
| 5 | HTML 內容 | `footer-description` 為空 div | index.html |
| 6 | 缺少組件 | 無 `.badge` / `.pill` 的 HTML 結構 | index.html |
| 7 | 缺少組件 | 無 `.avatar-container` + 下拉菜單結構 | index.html |

---

## 一、HTML 修改 — 按鈕 class 重命名

### 1.1 Hero Section 按鈕

**文件：** `index.html`

```html
<!-- 修改前 -->
<a href="#start" class="btn-hero-primary">Start for Free ...</a>
<a href="#demo" class="btn-hero-secondary">Watch Demo ...</a>

<!-- 修改後 -->
<a href="#start" class="btn btn-primary">Start for Free ...</a>
<a href="#demo" class="btn btn-secondary">Watch Demo ...</a>
```

---

### 1.2 Navbar 按鈕

**文件：** `index.html`

```html
<!-- 修改前 -->
<a href="#signin" class="btn-signin">Sign In</a>
<a href="#start" class="btn-start">Start for Free</a>

<!-- 修改後 -->
<a href="#signin" class="btn btn-ghost">Sign In</a>
<a href="#start" class="btn btn-dark">Start for Free</a>
```

---

## 二、CSS 修改 — 按鈕規則重構

**文件：** `styles.css`

### 2.1 Navbar 按鈕選擇器重命名

```css
/* 修改前 */
.btn-signin { ... }
.btn-signin:hover { ... }
.btn-start { ... }
.btn-start:hover { ... }

/* 修改後 */
.btn-ghost { ... }
.btn-ghost:hover { ... }
.btn-dark { ... }
.btn-dark:hover { ... }
```

---

### 2.2 Hero 按鈕：抽出 `.btn` 基礎類 + 重命名

```css
/* 修改前：共用樣式綁定在特定命名上 */
.btn-hero-primary,
.btn-hero-secondary {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    gap: 8px;
    font-size: 16px;
    font-weight: 500;
    padding: 14px 32px;
    border-radius: 28px;
    transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease;
    white-space: nowrap;
}

.btn-hero-primary { background: linear-gradient(...); color: #fff; ... }
.btn-hero-primary:hover { box-shadow: ...; transform: ...; }
.btn-hero-secondary { background-color: #fff; ... }
.btn-hero-secondary:hover { ... }

/* 修改後：公共樣式抽到 .btn，各型只保留差異 */
.btn {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    gap: 8px;
    font-size: 16px;
    font-weight: 500;
    padding: 14px 32px;
    border-radius: 28px;
    transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease;
    white-space: nowrap;
    cursor: pointer;
}

.btn-primary {
    background: linear-gradient(110deg, #504ff5 11%, #40c3fb 90%);
    color: #fff;
    box-shadow: 0 4px 15px rgba(80, 79, 245, 0.3);
}

.btn-primary:hover {
    box-shadow: 0 6px 20px rgba(80, 79, 245, 0.4);
    transform: translateY(-2px);
}

.btn-secondary {
    background-color: #fff;
    color: var(--primary-color);
    box-shadow: 0 4px 15px rgba(224, 224, 224, 0.3);
    border: 2px solid #e5e5e5;
}

.btn-secondary:hover {
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
    transform: translateY(-2px);
}
```

---

## 三、HTML 修改 — 結構問題修正

### 3.1 pain-cards 移回正確 section

目前 `pain-cards` 放在 `.contact` section 內，應移到 `.pain-point` section 內。

```html
<!-- 修改前（錯誤位置） -->
<section class="contact" id="contact">
    <div class="contact-container">
        <h2 class="section-title">Get in touch with us</h2>
        <div class="pain-cards">        <!-- ❌ 錯放在 contact 裡 -->
            <div class="pain-card"> ... </div>
            <div class="pain-card"> ... </div>
            <div class="pain-card"> ... </div>
        </div>
    </div>
</section>

<!-- 修改後（正確位置） -->
<section class="pain-point" id="pain-point">
    <div class="pain-point-container">
        <h2 class="section-title">Still Struggling with Job Applications?</h2>
        <div class="pain-cards">        <!-- ✅ 移到 pain-point 裡 -->
            <div class="pain-card">
                <span class="pain-icon">&blacklozenge;</span>
                <p>Don't know how to tailor your resume for each role</p>
            </div>
            <div class="pain-card">
                <span class="pain-icon">&acute;</span>
                <p>Freeze up when answering behavioural interview questions</p>
            </div>
            <div class="pain-card">
                <span class="pain-icon">&cap;</span>
                <p>Spend hours writing cover letters with no results</p>
            </div>
        </div>
    </div>
</section>

<section class="contact" id="contact">
    <div class="contact-container">
        <h2 class="section-title">Get in touch with us</h2>
        <!-- contact 內容待補 -->
    </div>
</section>
```

---

### 3.2 footer-description 補充文字

```html
<!-- 修改前 -->
<div class="footer-description">
    
</div>

<!-- 修改後 -->
<div class="footer-description">
    AI-powered career coaching for the next generation of tech talent.
</div>
```

---

## 四、新增組件 HTML（Task 3 要求）

以下組件結構插入 Hero section 後方，作為組件範例（加上 `<!-- Component Examples -->` 注釋）。

### 4.1 Badge / Pill

```html
<!-- Badge / Pill Component Examples -->
<div class="component-examples" style="padding: 20px; display: flex; gap: 12px; flex-wrap: wrap;">
    <span class="badge badge-primary">New</span>
    <span class="badge badge-secondary">Beta</span>
    <span class="badge badge-success">Active</span>
    <span class="pill">AI Powered</span>
    <span class="pill pill-outline">Free Trial</span>
</div>
```

對應 CSS（新增到 styles.css）：
```css
/* Badge / Pill */
.badge {
    display: inline-block;
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.03em;
}

.badge-primary {
    background-color: #504ff5;
    color: #fff;
}

.badge-secondary {
    background-color: #e5e5e5;
    color: #333;
}

.badge-success {
    background-color: #d1fae5;
    color: #065f46;
}

.pill {
    display: inline-block;
    padding: 6px 16px;
    border-radius: 999px;
    font-size: 13px;
    font-weight: 500;
    background-color: #f0f0ff;
    color: #504ff5;
}

.pill-outline {
    background-color: transparent;
    border: 1.5px solid #504ff5;
}
```

---

### 4.2 Avatar + 用戶下拉菜單

```html
<!-- Avatar + Dropdown Component Example -->
<div class="avatar-container">
    <div class="avatar">
        <img src="assets/avatar-placeholder.png" alt="User Avatar">
    </div>
    <div class="dropdown-menu">
        <a class="dropdown-item" href="#profile">My Profile</a>
        <a class="dropdown-item" href="#settings">Settings</a>
        <a class="dropdown-item dropdown-item-danger" href="#signout">Sign Out</a>
    </div>
</div>
```

對應 CSS（新增到 styles.css）：
```css
/* Avatar + Dropdown */
.avatar-container {
    position: relative;
    display: inline-block;
}

.avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    overflow: hidden;
    background-color: #e5e5e5;
    cursor: pointer;
}

.avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.dropdown-menu {
    position: absolute;
    top: calc(100% + 8px);
    right: 0;
    background-color: #fff;
    border: 1px solid #e5e5e5;
    border-radius: 12px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
    min-width: 160px;
    padding: 8px 0;
    display: none;
}

.avatar-container:hover .dropdown-menu {
    display: block;
}

.dropdown-item {
    display: block;
    padding: 10px 16px;
    font-size: 14px;
    color: var(--text-color);
    text-decoration: none;
    transition: background-color 0.2s ease;
}

.dropdown-item:hover {
    background-color: #f4f4f4;
}

.dropdown-item-danger {
    color: #dc2626;
}
```

---

## 五、修改順序建議

```
1. styles.css — 重構按鈕規則（抽 .btn 基礎類，重命名選擇器）
2. index.html — 更新 Navbar 按鈕 class 屬性
3. index.html — 更新 Hero 按鈕 class 屬性
4. index.html — 移動 pain-cards 到正確 section
5. index.html — 補充 footer-description 文字
6. styles.css + index.html — 新增 Badge/Pill 組件
7. styles.css + index.html — 新增 Avatar + Dropdown 組件
```
